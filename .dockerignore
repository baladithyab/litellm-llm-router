# =============================================================================
# Docker Build Exclusions for LiteLLM + LLMRouter
# =============================================================================
# Reduces build context size and speeds up Docker builds
# Keep in sync with files needed by docker/Dockerfile

# Git and version control
.git
.gitignore
.gitmodules
.github

# Documentation (not needed in container)
docs/
*.md
!README.md

# Tests (not needed in production image)
tests/
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# Development files
.vscode/
.idea/
*.swp
*.swo
*~

# Python artifacts
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.hypothesis/

# Virtual environments
.venv/
venv/
ENV/
env/
.pyenv/
.python-version

# Build artifacts
build/
dist/
eggs/
*.egg-info/
*.egg
*.whl
*.tar.gz

# Docker files (prevent nested contexts)
docker-compose*.yml
Dockerfile*
!docker/Dockerfile
!docker/Dockerfile.local

# Environment and secrets
.env
.env.*
*.local
secrets/
*.key
*.pem

# IDE and editor
.idea/
*.iml
.project
.classpath
.settings/

# OS files
.DS_Store
Thumbs.db
*.log

# Examples (not needed in main image)
examples/
notebooks/
*.ipynb

# Reference libraries (large, built separately)
reference/

# Project planning and specs (not needed in container)
plans/
.kiro/

# Prisma migrations (client generated at build time)
prisma/

# MLOps artifacts (handled separately)
models/*.pt
models/*.pkl
data/
mlruns/
artifacts/

# Scripts not needed in container
scripts/

# Custom routers dir (mounted at runtime, not baked in)
custom_routers/

# Misc
LICENSE
CHANGELOG.md
CONTRIBUTING.md
Makefile
*.tgz
lefthook.yml
.pre-commit-config.yaml
.yamllint.yaml
