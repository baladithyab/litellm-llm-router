name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Removed IMAGE_NAME and TEST_PORT env vars - Docker builds moved to docker-build.yml

jobs:
  # Detect what changed to optimize CI
  changes:
    runs-on: ubuntu-latest
    outputs:
      python: ${{ steps.filter.outputs.python }}
      compose: ${{ steps.filter.outputs.compose }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            python:
              - 'src/**'
              - 'tests/**'
              - 'requirements.txt'
              - 'pyproject.toml'
            compose:
              - 'docker-compose*.yml'
              - 'examples/mlops/docker-compose*.yml'

  lint:
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.python == 'true' || github.event_name == 'push' }}
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "0.9.26"
          enable-cache: true

      - name: Run ruff check
        run: uv run --with ruff ruff check src/

      - name: Check formatting with ruff
        run: uv run --with ruff ruff format --check src/

  test:
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.python == 'true' || github.event_name == 'push' }}
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          version: "0.9.26"
          enable-cache: true

      - name: Install dependencies
        run: |
          uv pip install --system git+https://github.com/ulab-uiuc/LLMRouter.git
          uv pip install --system litellm pytest pytest-asyncio
          uv pip install --system -r requirements.txt

      - name: Run unit tests
        run: |
          uv run pytest tests/test_strategies.py -v --tb=short
        env:
          PYTHONPATH: src

  # Docker build, smoke tests, and security scans are handled in docker-build.yml
  # This workflow focuses on Python linting, unit tests, and compose validation

  validate-compose:
    runs-on: ubuntu-latest
    needs: changes
    if: ${{ needs.changes.outputs.compose == 'true' || github.event_name == 'push' }}
    steps:
      - uses: actions/checkout@v4

      - name: Validate docker-compose.yml
        run: docker compose -f docker-compose.yml config

      - name: Validate docker-compose.ha.yml
        run: docker compose -f docker-compose.ha.yml config

      - name: Validate MLOps compose
        run: docker compose -f examples/mlops/docker-compose.mlops.yml config
